{{#inArray features 'db'}}import type { ClientConfig } from 'pg';
{{/inArray}}
import type { ConfigurationSchema } from '@openapi-typescript-infra/service';

export interface {{properCase name}}ConfigSchema extends ConfigurationSchema {
{{#inArray features 'db'}}
  db: ClientConfig;
{{/inArray}}
}
