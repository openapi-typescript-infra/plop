DO
$body$
BEGIN
   IF NOT EXISTS (
      SELECT *
      FROM   pg_catalog.pg_user
      WHERE  usename = '{{dashCase name}}') THEN

      CREATE USER "{{dashCase name}}" WITH PASSWORD '{{dashCase name}}-pw';
   END IF;
END
$body$;
